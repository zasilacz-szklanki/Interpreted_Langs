# DB Seeding

1. Go to the backend folder:
   `cd backend`

2. Run the seed script directly:
   `npx ts-node prisma/seed.ts`

---
**Note:** If you want to populate the database from scratch, run this command first:
`npx prisma migrate reset`